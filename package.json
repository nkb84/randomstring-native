{
  "name": "randomstring-native",
  "version": "1.0.0",
  "description": "Generate multiple random string by native code",
  "main": "build/Release/randomstring-native.node",
  "dependencies": {
    "node-addon-api": "3.0.2"
  },
  "devDependencies": {},
  "scripts": {
    "build": "node-gyp configure && node-gyp build --release",
    "build-debug": "node-gyp configure && node-gyp build --debug",
    "clean": "node-gyp clean"
  },
  "keywords": ["random", "performance"],
  "author": "nkb84",
  "license": "MIT"
}
